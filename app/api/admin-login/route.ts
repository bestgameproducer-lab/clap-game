import { NextResponse } from 'next/server'; import { sign } from '@/lib/session'; import crypto from 'crypto';
export async function POST(req:Request){const {password}=await req.json(); const expected=process.env.ADMIN_PASSWORD||''; const a=Buffer.from(String(password)); const b=Buffer.from(expected); const ok=a.length===b.length&&crypto.timingSafeEqual(a,b); if(!ok)return NextResponse.json({error:'密码错误'},{status:401}); const res=NextResponse.json({ok:true}); res.cookies.set('admin_session',sign('admin'),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:60*60*12,path:'/'}); return res;}
